# Phase H1 — current-turn authority + STANDARD agency

Frozen PR #560 Gemini → DeepSeek handoff fixture, reassembled with H1 production owners.

- `source-fixtures/` and `gemini-history/` are unmodified copies from PR #560.
- `assembled/` is the new production assembly (packet heuristics omitted; current-user authority; STANDARD/OFF agency).
- Live DeepSeek RAWs (`H1R1`/`H1R2`/`H1R3`) are recorded only after `ACCEPTANCE.json` passes.
- `QUALITY_SCORE_ASSIGNED=false`. Human review of RAW only.
