# Railway / Nixpacks — better-sqlite3 native compile (node-gyp needs Python + make)
# Node 22.12+ required by @exodus/bytes (isomorphic-dompurify/jsdom chain); nodejs_22 in this nixpkgs = 22.12.0.
[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]
